json-source:
  url: https://jsonplaceholder.typicode.com
spring:
  datasource:
    url: jdbc:h2:mem:vk
    driverClassName: org.h2.Driver
    username: user
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database: h2
    open-in-view: false
  h2:
    console:
      enabled: true
